# Core Deep Learning (Install via conda)
# conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia

# Scientific Computing (Install via conda)
# conda install numpy scipy pandas matplotlib seaborn

# Optimization Libraries (Install via pip)
optuna>=3.0.0
scikit-optimize>=0.9.0
scikit-learn>=1.3.0

# Utilities (Install via pip)
pyyaml>=6.0
tqdm>=4.65.0
tensorboard>=2.12.0
plotly>=5.14.0

# Note: This file is for reference. Use conda commands above for installation.